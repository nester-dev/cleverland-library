export enum UrlConfig {
  CATEGORIES = "categories",
  BOOKS = "books",
  REGISTER = "auth/local/register",
  LOGIN = "auth/local",
  COMMENTS = "comments",
  BOOKING = "bookings",
  USER = "users",
  UPLOAD = "upload",
}
